<?php

namespace App\Http\Controllers;

/**
 * Base Controller — All other controllers in the app extend this one.
 * Laravel uses this as a shared parent where you can add methods or
 * middleware that every controller should have access to.
 */
abstract class Controller
{
    //
}
